node_modules/
dist/
build/

# Environment / secrets
.env
.env.*.local
.env.local

# Logs
*.log
npm-debug.log*

# Prisma
prisma/migrations/dev.db

# Testing
coverage/

# Docker (keep the Dockerfile itself, just ignore local overrides)
docker-compose.override.yml

# Spec PDFs kept outside the repo (confidential / large)
docs/

# Local tooling / doc extracts (not part of the app)
.tmp-docs-extract/
.venv-pdf/

# macOS
.DS_Store
