/**
 * Seed static data: league tiers, badges, pilot masjid.
 * Run: npm run prisma:seed
 */
async function main(): Promise<void> {
  console.info('Seed stub — implement league_tiers, badges, Khizra Masjid (Part 6 / Part 11).');
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
